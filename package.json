{
  "name": "sp-playcount-worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "base32-encode": "^2.0.0",
    "hono": "^4.8.4",
    "totp-generator": "^1.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.42.2"
  }
}